DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="change-me"
NEXTAUTH_URL="http://localhost:3000"

# Opcional: senha inicial do usuário admin criado pelo seed (prisma/seed.ts).
# Se não definida, usa "jetaflow123" (apenas para desenvolvimento local).
# Defina um valor forte antes de rodar o seed em produção.
SEED_ADMIN_PASSWORD=
